using PathwayChoice.Models;

namespace PathwayChoice.Models
{
    /*public class EFTeacherRepository: ITeacherRepository
    {
        private TeacherContext _context;

        public EFTeacherRepository(TeacherContext context)
        {
            _context = context;
        }



        public IQueryable<Teacher> Teachers => _context.Entertainers;


        public void Add<T>(T entity) where T : class
        {
            _context.Set<T>().Add(entity);
        }

        public void Remove<T>(T entity) where T : class
        {
            _context.Set<T>().Remove(entity);
        }

        public void Update<T>(T entity) where T : class
        {
            _context.Set<T>().Update(entity);
        }

        public void SaveChanges()
        {
            _context.SaveChanges();
        }


    }*/
}
